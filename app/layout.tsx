import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "MLAZ LIMITED",
  description: "Guaranteed amble across the globe — Footwear Sales, Inventory & Business Management Platform",
  keywords: ["footwear", "business management", "inventory", "sales", "MLAZ LIMITED"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
